# Server-only — used by Server Actions / Route Handlers, never sent to the browser.
API_URL=http://localhost:5000/api

# Public — inlined into the client bundle at build time, safe to expose.
NEXT_PUBLIC_SITE_URL=https://www.elvionstech.com
